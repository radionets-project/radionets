---
name: radionets
channels:
    - fastai
    - pytorch
    - defaults
dependencies:
  - python>=3.6
  - pytorch>=1.6
  - torchvision<0.10.0
  - cudatoolkit=10.2
  - cartopy
  - pip
  - pip:
    - -e .
